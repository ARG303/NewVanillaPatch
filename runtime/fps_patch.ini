[fps_patch]
; v3 diagnostic note: target is compile-time fixed to 60 because the patch is
; applied during DLL_PROCESS_ATTACH, before safe config file I/O.
target_fps=60

; package_version=7.0.1 (metadata-only update test; FPS logic unchanged)
