NewVanilla Patch v0.8.10 — RA3BattleNet SkuDef compatibility hotfix

Upload these paths to the repository in ONE commit:
  LAUNCHER_VERSION
  launcher/RA3FPSLauncher.exe
  installer/NewVanillaPatch_Setup.exe
  .github/workflows/update-manifest.yml
  compatibility/ra3_40023_to_29aa.r3dp
  compatibility/ra3_ec230_to_40023.r3np

Do NOT replace MOD_VERSION or mod/NewVanilla.zip.
Wait for Actions -> Update manifest to complete successfully.

Important v0.8.10 change:
  - no RA3FPSLauncher.clean.cfg is created or passed to RA3;
  - -config points to the original RA3_*_1.12.SkuDef filename/path;
  - if a CommunityPatch add-config line exists, the original SkuDef is backed up,
    temporarily sanitized in place, and restored after RA3 exits/recovery.
